# 项目初始化与排障记录

## 环境信息

- 项目路径：`E:\nat\QQbot\hello-world`
- Python 版本：>= 3.10, < 4.0
- 包管理：`pip` + `pyproject.toml`

## 已完成操作

### 1. 修复项目名并安装项目

原项目名不符合包名规范，导致 `pip install -e .` 安装失败。  
已将 `pyproject.toml` 中的 `name` 改为：

```toml
name = "hello-world"
```

随后以可编辑模式重新安装成功。

### 2. 安装插件直接依赖的第三方包

项目核心依赖由 `pyproject.toml` 管理，但部分插件运行时还需要额外包，已直接安装：

- `dashscope`
- `ddgs`
- `nonebot-plugin-apscheduler`
- `python-dotenv`

安装命令：

```powershell
E:\nat\QQbot\hello-world\.venv\Scripts\pip.exe install dashscope ddgs nonebot-plugin-apscheduler python-dotenv
```

> 注：`dateparser` 曾在提醒功能开发期间安装；后来提醒功能整体移除，`dateparser` 已不再需要。

### 3. 解决 `ModuleNotFoundError: No module named 'nonebot'`

运行 `bot.py` 时出现该错误，原因是当前使用的不是项目虚拟环境中的 Python。  
正确运行方式：

```powershell
E:\nat\QQbot\hello-world\.venv\Scripts\python.exe E:\nat\QQbot\hello-world\bot.py
```

### 4. 修复 `ai_chat` 插件报错 `NameError: name 'scheduler' is not defined`

问题表现：  
运行 `nb run` 启动项目时，`src/plugins/ai_chat/__init__.py` 加载失败，traceback 指向：

```python
from nonebot_plugin_apscheduler import scheduler
```

根因：  
插件代码在模块顶层 import `scheduler`，但 NoneBot 尚未按插件方式加载并初始化 `nonebot_plugin_apscheduler`，导致 `scheduler` 未定义。

修复方式：  
1. 在 `pyproject.toml` 的 `[project]` 里显式声明依赖：
```toml
nonebot-plugin-apscheduler>=0.5.0
```
2. 在 `bot.py` 的插件加载顺序中，先加载 `nonebot_plugin_apscheduler`，再加载本地插件：
```python
nonebot.load_plugin("nonebot_plugin_apscheduler")
nonebot.load_plugin("nonebot_plugin_quark")
```

### 5. 修复面板 `ValueError: NoneBot has not been initialized`

问题表现：  
`panel/app.py` 直接 import `src/plugins/ai_chat/__init__.py`，而 `ai_chat` 内部在模块顶层执行了 `from nonebot_plugin_apscheduler import scheduler`，导致 NoneBot 未初始化就报错。

根因：  
`panel` 是独立进程，和 Bot 主进程分离，不应共享任何 NoneBot 插件代码。

修复方式：  
重写 `panel/app.py`，完全移除对 NoneBot 插件模块的依赖：
- 不再 `import` 项目内部的插件代码
- 直接读写 JSON 文件获取配置和记忆数据
- 直接读写 `.env` 文件修改配置
- 使用 FastAPI + uvicorn 作为独立服务

### 6. 修复面板 Jinja2 模板引擎报错 `TypeError: unhashable type: 'dict'`

问题表现：  
面板启动后访问页面返回 500 Internal Server Error，日志显示 Jinja2 缓存相关的 `TypeError: unhashable type: 'dict'`。

根因：  
Jinja2 模板缓存键使用字典对象，但 Starlette/FastAPI 传入的 `Request` 对象不可哈希。

修复方式：  
移除 Jinja2 模板引擎，改为直接读取 HTML 文件并通过 `HTMLResponse` 返回。

### 7. 端口迁移记录

| 时间 | 端口 | 原因 |
|------|------|------|
| 初始 | 8080 | 常用端口，常被占用 |
| 第一轮 | 8081 | 8080 被旧进程占用无法释放 |
| 第二轮 | 8082 | 8081 被旧进程占用无法释放 |
| 第三轮 | 8083 | 8082 被旧进程占用无法释放 |
| 最终 | **30080** | 选取大号高位端口，避免与常用端口冲突 |

---

## 环境初始化脚本

已创建 `setup.bat`，功能如下：

1. 检查 Python 是否可用
2. 若 `.venv` 不存在则创建虚拟环境
3. 激活虚拟环境并升级 `pip`
4. 以可编辑模式安装项目依赖：`pip install -e .`
5. 安装 `nb-cli` 及运行时依赖：`nb-cli`、`dashscope`、`ddgs`、`nonebot-plugin-apscheduler`、`python-dotenv`
6. 自动生成 `run.bat`
7. 输出后续启动命令

使用方式：

```powershell
E:\nat\QQbot\hello-world\setup.bat
```

## 启动脚本

已新增 `run.bat`，使用方式：

```powershell
E:\nat\QQbot\hello-world\run.bat
```

或直接使用：

```powershell
nb run
```

---

## 管理面板（Bot Management Panel）

### 简介

管理面板是一个独立的 Web 界面，用于可视化管理和监控 Bot。

**功能页面：**
- **数据看板**：Bot 运行状态、实体数量、消息统计、管理员数量
- **API 配置**：DeepSeek API Key、Base URL、模型名称、系统提示词
- **管理员管理**：添加/删除私聊管理员（即使生效）
- **记忆管理**：查看所有记忆实体、查看详情、一键导出
- **功能状态**：查看各功能（Sudo、搜索、图片理解等）的启用状态

### 启动方式

1. **Bot**：`run.bat`
2. **管理面板**：`panel/run.bat`

面板地址：`http://localhost:30080`

### 登录凭证

- **用户名**：`seahorse`
- **密码**：`123456`

> ⚠️ 首次使用后建议修改 `panel/app.py` 中的 `PANEL_USERNAME` 和 `PANEL_PASSWORD`

### 技术方案

- **后端**：FastAPI + uvicorn
- **前端**：单页面 HTML + Tailwind CSS + Vanilla JS
- **无数据库**：使用 JSON 文件存储会话和管理员数据
- **无构建工具**：纯 Python + 原生 HTML/JS

### 文件结构

```
panel/
├── app.py          # FastAPI 后端
├── run.bat         # 启动脚本
├── templates/
│   ├── login.html  # 登录页
│   └── index.html  # 主页面
└── static/
    ├── style.css   # 样式
    └── app.js      # 前端逻辑
```

---

## 后续可继续优化的点

- [x] 添加启动脚本 `run.bat`
- [x] 管理面板（独立 Web 界面）
- [x] 管理员即时增删
- [x] 记忆导出
- [x] 面板 Bot 状态真实检测
- [x] 数据看板金融榜风格 + 折线图
- [x] 记忆管理个人/群聊 Tab + 卡片布局 + TXT 导出
- [ ] 检查 `.env` / `.env.prod` 配置是否齐全
- [ ] 确认 OneBot 正向/反向 WebSocket 配置
- [ ] 配置 `.gitignore` 忽略 `.venv`、`data/`、`memory/` 等
- [ ] 管理面板添加修改背景功能
- [ ] API 配置修改后即时生效（无需重启 Bot）

---

## 后续可继续优化的点（续）

### 8. 面板大幅优化

**Bot 真实状态检测**：不再返回假数据 `running`，改为用 `tasklist`（Windows）或 `pgrep`（Linux）检测 python.exe / bot.py 进程是否存活。

**数据看板金融榜风格重构**：
- 移除"快速操作"区域
- Bot 状态栏改为顶部独立一行，含绿色脉冲点 + 文字 + 刷新按钮
- 周期选择：今天 / 3天 / 7天 / 14天 Tab 切换
- 4 张渐变色数据卡片：实体总数、记忆消息、管理员、总回复条数（本周期内）
- 左右两个 Chart：消息量柱状图 + 总字数折线图，通过 `/api/chart` 获取每日数据（按消息 timestamp 筛选）
- Chart.js 绘制，K 线配色风格

**记忆管理重构**：
- 改为顶部 Tab 切换（个人 / 群聊）
- 列表从表格改为卡片网格布局，每张卡片显示 ID、消息数、总结数
- 修复 summary 内容读取：从 meta.json 读文件路径和 offset，直接读对应 .txt 文件内容
- 导出改为 `.txt` 纯文本格式，附带分隔线和时间戳
- 支持按 type 筛选（`/api/memory?type=user` / `type=group`）

### 9. run.bat 与 Bot 联动

面板不再独立"假设" Bot 在运行，而是真实调用系统 API 检测 Bot 进程。

---

> 最后更新：2026-07-11
